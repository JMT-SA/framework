# frozen_string_literal: true

ProductSchema = Dry::Validation.Form do
  optional(:id).filled(:int?)
  required(:product_type_id).filled(:int?)
  optional(:variant).maybe(:str?)
  optional(:style).maybe(:str?)
  optional(:assembly_type).maybe(:str?)
  optional(:market_major).maybe(:str?)
  optional(:commodity).maybe(:str?)
  optional(:variety).maybe(:str?)
  optional(:ctn_size_basic).maybe(:str?)
  optional(:ctn_size_old_pack).maybe(:str?)
  optional(:pls_pack_code).maybe(:str?)
  optional(:fruit_mass_nett_kg).maybe(:decimal?)
  optional(:holes).maybe(:str?)
  optional(:perforation).maybe(:str?)
  optional(:image).maybe(:str?)
  optional(:length_mm).maybe(:decimal?)
  optional(:width_mm).maybe(:decimal?)
  optional(:height_mm).maybe(:decimal?)
  optional(:diameter_mm).maybe(:decimal?)
  optional(:thick_mm).maybe(:decimal?)
  optional(:thick_mic).maybe(:decimal?)
  optional(:colour).maybe(:str?)
  optional(:grade).maybe(:str?)
  optional(:mass).maybe(:str?)
  optional(:material_type).maybe(:str?)
  optional(:treatment).maybe(:str?)
  optional(:specification_notes).maybe(:str?)
  optional(:artwork_commodity).maybe(:str?)
  optional(:artwork_marketing_variety_group).maybe(:str?)
  optional(:artwork_variety).maybe(:str?)
  optional(:artwork_nett_mass).maybe(:str?)
  optional(:artwork_brand).maybe(:str?)
  optional(:artwork_class).maybe(:str?)
  optional(:artwork_plu_number).maybe(:decimal?)
  optional(:artwork_other).maybe(:str?)
  optional(:artwork_image).maybe(:str?)
  optional(:marketer).maybe(:str?)
  optional(:retailer).maybe(:str?)
  optional(:supplier).maybe(:str?)
  optional(:supplier_stock_code).maybe(:str?)
  optional(:product_alternative).maybe(:str?)
  optional(:product_joint_use).maybe(:str?)
  optional(:ownership).maybe(:str?)
  optional(:consignment_stock).maybe(:bool?)
  optional(:start_date).maybe(:date?)
  optional(:end_date).maybe(:date?)
  optional(:remarks).maybe(:str?)
end
